{
  "name": "filemate - devcontainer",
  "dockerComposeFile": ["../docker-compose.yml"],
  "service": "frontend",
  "workspaceFolder": "/workspaces/filemate",
  "forwardPorts": [3000],
  "extensions": [
    "ms-azuretools.vscode-docker",
    "esbenp.prettier-vscode",
    "dbaeumer.vscode-eslint"
  ],
  "postCreateCommand": "bash -lc 'chown -R node:node /workspaces/filemate && su node -c \"cd frontend && npm install && npx cypress verify\"'",
  "remoteUser": "root"
}